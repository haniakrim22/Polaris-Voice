/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'localhost',
      'polarisvoice.com',
      'api.openai.com',
    ],
  },
}

module.exports = nextConfig